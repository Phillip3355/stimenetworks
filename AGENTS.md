# Local environment files

- Do not open, read, print, copy, or modify `.env.local` unless the user explicitly asks for a specific change to that file.
- Never display values from `.env.local` in tool output or responses.
